# Copy to .env.local for local development. In Vercel, set these under
# Project → Settings → Environment Variables (no VITE_ prefix for server-side keys).

# ─── Contact form (api/contact.ts, Resend) ─────────────────────────────────────
# Replace re_xxxxxxxxx with your real Resend API key (from https://resend.com/api-keys).
# Set RESEND_API_KEY in Vercel and in .env.local — never commit the key.
RESEND_API_KEY=re_xxxxxxxxxxxx
CONTACT_EMAIL=contact@shankstrategy.com
# Optional: "From" address (must be verified in Resend if not using sandbox)
# RESEND_FROM_EMAIL=website@yourdomain.com

# ─── Client-side (Vite exposes only VITE_* to the browser) ───────────────────
# Maps (Map.tsx) – optional
# VITE_FRONTEND_FORGE_API_KEY=
# VITE_FRONTEND_FORGE_API_URL=
